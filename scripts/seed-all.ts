/**
 * Legacy seed script.
 *
 * The project now runs fully from Firestore and no longer keeps local
 * curriculum source files. Reseeding must be done from an external export.
 */

console.log(
  "seed-all.ts is disabled: local curriculum files were removed. " +
  "Use Firestore export/import or a JSON-based ingestion script."
);
